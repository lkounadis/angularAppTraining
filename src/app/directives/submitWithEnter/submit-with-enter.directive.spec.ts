import { SubmitWithEnterDirective } from './submit-with-enter.directive';

describe('SubmitWithEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new SubmitWithEnterDirective();
    expect(directive).toBeTruthy();
  });
});
